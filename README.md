## Instructions to run
### Backend
```
python3 -m venv ./.venv
source ./.venv/bin/activate
pip install -r django-backend/requirements.txt
python manage.py runserver
``` 

### Frontend
```
yarn install
yarn start
```